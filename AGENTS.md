# Repository Agent Instructions

1. 本環境已安裝 Playwright Chromium。
2. UI 測試時，請使用 `serve-static-8000` 啟動靜態網站。
3. 請使用 `screenshot-url` 擷取畫面。
4. 不要只用 `which chromium`、`which google-chrome` 或 `which firefox` 判斷環境是否具備瀏覽器。
5. 完成前至少擷取一張桌面版畫面，確認頁面正常載入。
6. 如果修改涉及響應式介面，再擷取一張手機版畫面。
